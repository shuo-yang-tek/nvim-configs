snippet rj "res.json({...})" i
res.json({
   message: 'success',
   $0
});
endsnippet

snippet rsj "res.status(...).json({...})" i
res.status(${1:/* ... */}).json({
   message: '${2:/* ... */}',
   $0
});
endsnippet

snippet ajv "create ajv schema" i
{
   required: [${1:/* ... */}],
   properties: {$0`!p

import re
snip >> 2

keys = re.findall("(?:')([a-zA-Z0-9_']+)(?:')", t[1])
if len(keys) == 0:
   snip.rv = ''

for key in keys:
   snip += '{}: {{'.format(key)
   snip += '},'

`
   }
}
endsnippet
